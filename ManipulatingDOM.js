
        // get element by id 

        let main = document.getElementById('main');
        console.log(main);

        // get element by class 

        let containers = document.getElementsByClassName('container');
        console.log(containers);

        // selector 

        // let selector0 = document.querySelector('.container');
        // console.log('Selector Returns', selector0);

        let selector1 = document.querySelectorAll('.container');
        console.log('Selector Returns', selector1);
